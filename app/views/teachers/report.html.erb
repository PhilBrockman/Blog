<%= render partial: "special_education" if @teacher.special_education %>

<% if @teacher.certificate_location.upcase == "PA" %>


<% end %>

For your role (<%= @role.name %>), you need the following <%= pluralize @role.credentials.length, "certificate" %>:

<%= render partial: "credentials/credential_table", locals: {credentials: @role.credentials} %>