<!-- 
Commented because at this point, all info about special education certificates should already be included with the teacher information.
	<%= render partial: "special_education" if @teacher.special_education %> -->

<p>For your role (<%= @role.name %>), you need <%= pluralize @role.credentials.length, "certificate" %>.  If you would like to be emailed a copy of these results, look below the table for instructions.</p>

<%= render partial: "credentials/local_notification" %>
<%= render partial: "credentials/credential_table", locals: {credentials: @role.credentials, active: @id_list} %>