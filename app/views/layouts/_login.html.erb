<% if admin_signed_in? %>
  <li>
  <%= link_to('Logout', destroy_admin_session_path, :method => :delete) %>        
  </li>
<% else %>
  <%= link_to('Admin Login', new_admin_session_path)  %> 
<% end %>
<br />
